# Simple KOA + TS + Bun boilerplate

To install dependencies:

```bash
git clone git@github.com:danegigi/koa-startup.git <appname>
rm -rf <appname>/.git
```

To install and run:

```bash
bun install
bun run dev
```
